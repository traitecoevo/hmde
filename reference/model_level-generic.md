# generic model_level setter

generic model_level setter

generic model_level setter

generic obs_data setter

generic obs_data setter

## Usage

``` r
model_level(x)

model_level(x) <- value

obs_data(x)

obs_data(x) <- value
```

## Arguments

- x:

  hmde hmde_data_template

- value:

  character string
